sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docker-conpose exec --env 'RAILS_ENV=test' web rails db:create
  - docker-conpose exec --env 'RAILS_ENV=test' web rails db:migrate
  - docker-conpose exec --env 'RAILS_ENV=test' web rails test
